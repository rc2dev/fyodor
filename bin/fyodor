#!/usr/bin/env ruby

require "fyodor/cli"

# Pass --trace to get the stack trace
if ARGV.include?("--trace")
  ARGV.delete("--trace")
  Fyodor::CLI.new.main
else
  begin
    Fyodor::CLI.new.main
  rescue StandardError => e
    abort(e.message)
  end
end
